# Tasks

- [x] 1.1 Install a capturing pointerdown listener in the page template that blurs a focused input, textarea, or select when the tap is outside a form control
- [x] 1.2 Add a Done control to the composer, shown only while the keyboard is visible (removed again by `composer-compact`: the tap-outside listener makes it redundant)
- [x] 1.3 Cover the listener and the control in the mobile and shell tests
- [ ] 1.4 Confirm on the phone that tapping outside a draft restores the tab bar
